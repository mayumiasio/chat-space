.chat-side
  .chat-side__header
    .chat-side__header_box
      = current_user.name
      %ul.header__lists
        %li.list
          = link_to new_group_path do
            = fa_icon 'pencil-square-o', class: 'icon'
        %li.list
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog', class: 'icon'
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。
-# .chat-side
-#   .chat-side__header
-#     .chat-side__header_box
-#       %h3.header__name
-#       = current_user.name
-#       %ul.side-header_box_menu
-#         %li.side-header_box_menu_new-group
-#           = link_to new_group_path  do
-#             = fa_icon 'edit' 
-#         %li.side-header_box_menu_new-group
-#           = link_to edit_user_path(current_user) do
-#             = fa_icon 'cog', class: 'icon'
-#         %li.side-header_box_menu_new-group
-#   .chat-side__groups
-#     -# .chat-side__groups_box
-#     - current_groups_name.each do |group|
-#     .group
-#       = link_to current_group_name.path(group) do
-#       .group__name
-#       = group.name
-#     .group__message
      メッセージはまだありません。